{
  "name": "ai-workbench-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ai-workbench": "./dist/src/tui/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/tui/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
